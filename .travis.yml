before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libxml2-utils
language: node_js
sudo: false
node_js:
  - "6"
  - "node"
  - "lts/*"
install:
  - npm install
test:
  - npm test
